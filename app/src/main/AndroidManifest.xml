<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.JiangHu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GuideActivity"  android:label="江湖" android:theme="@style/AppTheme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:theme="@style/AppTheme.Transparent"></activity>
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".LoginActivity" android:theme="@style/AppTheme.Transparent"></activity>
        <activity android:name=".MyProfileActivity"></activity>
        <activity android:name=".ItemDetailActivity"></activity>
        <activity android:name=".ItemListActivty"></activity>

        <!--<activity android:name=".ViewPagerActivity"></activity>-->
    </application>

</manifest>